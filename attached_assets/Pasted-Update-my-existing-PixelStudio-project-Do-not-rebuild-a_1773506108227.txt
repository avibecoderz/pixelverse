Update my existing PixelStudio project. Do not rebuild anything from scratch.

Add a QR code feature to the existing invoice so each invoice includes a QR code that opens the client’s photo gallery.

Project stack:
- Frontend: React + Vite
- Backend: Node.js + Express + Prisma + MySQL

Current system:
- Each client already has a private gallery link generated by the backend
- Gallery link format: https://yourdomain.com/gallery/{token}
- Clients use that link to view and download their photos

What I want:
- When an invoice is generated or viewed, show a QR code on the invoice
- The QR code should point to the client’s gallery link
- When the client scans the QR code, it should open their gallery page

Tasks:

Backend:
1. Make sure each client or gallery has a unique gallery token in the database
2. Update the invoice API response to include the gallery URL
3. Gallery URL format should be:
   https://yourdomain.com/gallery/{token}
4. Return the gallery URL together with the invoice details

Frontend:
1. Install and use a React QR code library such as react-qr-code or qrcode.react
2. Update the existing invoice preview page/component
3. Show:
   - Studio name
   - Invoice number
   - Client name
   - Service type
   - Photo format
   - Amount
   - Payment status
   - Date
4. Add a QR code section labeled:
   Scan to View Your Photos
5. Under the QR code, add this text:
   Scan this QR code to access and download your photos.
6. Make the QR code a reusable component

Print/PDF:
1. Make sure the QR code is visible and scannable when printing the invoice
2. QR code size should be at least 150px
3. Keep the layout clean for both screen and print
4. Make sure the QR code also appears when exporting the invoice as PDF

Important:
- Extend the existing project only
- Do not change the whole structure
- Keep the code modular and clean
- Reuse the current invoice module and UI
- Use the current backend and frontend flow

Also explain clearly:
- which existing files should be updated
- where to place the reusable QR code component in the React project
- how the backend should pass the gallery URL to the invoice component
- how to keep the QR code visible in print and PDF output